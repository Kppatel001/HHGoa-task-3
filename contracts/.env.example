# Only needed to deploy to a PUBLIC testnet (e.g. Sepolia).
# Local Hardhat needs none of this.
SEPOLIA_RPC_URL=
# A FUNDED test-ETH private key. NEVER a real key. NEVER commit the real .env.
DEPLOYER_PRIVATE_KEY=
