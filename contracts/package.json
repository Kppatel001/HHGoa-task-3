{
  "name": "faceproof-contracts",
  "version": "1.0.0",
  "description": "ContentVerification smart contract + Hardhat tooling for FaceProof",
  "license": "MIT",
  "private": true,
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "solc": "^0.8.24"
  }
}
